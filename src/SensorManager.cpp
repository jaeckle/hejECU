// src/SensorManager.cpp

#include "SensorManager.h"
#include "AppLogger.h"
#include <Arduino.h>

// =========================================================
// 1. GLOBALE VARIABLEN DEFINITIONEN
// =========================================================

// Definition des Laufzeit-Zustands
SensorState sensorState;

// =========================================================
// 2. FUNKTIONEN (Minimal Implementierung)
// =========================================================

// Definition der fehlenden Funktion aus Aktion 105
void initializeSensors()
{
    LOG(LOG_INFO, "SensorManager initialisiert.");
    // Logik wird später gefüllt (z.B. pinMode(ADC_PIN, INPUT);)
}

void updateSensorReadings()
{
    // Logik wird später gefüllt
}